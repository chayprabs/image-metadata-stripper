{
  "name": "@exifscrub/cli",
  "version": "1.0.0",
  "description": "CLI for ExifScrub metadata scrubbing",
  "type": "module",
  "bin": {
    "exifscrub": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "lint": "echo ok",
    "test": "pnpm run build && vitest run"
  },
  "dependencies": {
    "@exifscrub/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  },
  "license": "MIT"
}
